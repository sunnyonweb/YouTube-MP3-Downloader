version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      - FFMPEG_LOCATION=/usr/bin
    volumes:
      - downloads:/app/backend/downloads
    restart: unless-stopped

volumes:
  downloads:
    driver: local
